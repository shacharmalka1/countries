import React from "react";
import ReactDOM from "react-dom";
import OptionsDropdown from "./core/OptionDropdown";
ReactDOM.render(
  <React.StrictMode>
    <OptionsDropdown />
  </React.StrictMode>,
  document.getElementById("root")
);
